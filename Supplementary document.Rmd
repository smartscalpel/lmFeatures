---
title: "Supplementary document"
author: "Anatoly Sorokin"
date: "6/15/2017"
output:
  pdf_document:
    keep_tex: yes
    number_sections: yes
    highlight: tango
    toc: yes
    include:
      in_header: myStyle.sty
params:
  format: !r if(opts_knit$get("rmarkdown.pandoc.to") == 'html') c('screen', 'print')
    else 'print'
  version: !r if(nchar(Sys.which("git"))) system("git describe --long --dirty --abbrev=10  --tags  --always",
    intern=TRUE) else date()
---
```{r loadPackages, include=FALSE, cache=FALSE}
## load additional packages in this chunk
library(pander)
library(knitr)
library('Matrix')
library(ggplot2)
library(data.table)
library(plyr)
library(xtable)
library("FactoMineR")
library(cluster)
library(dendextend)
library(factoextra)
library(corrplot)
library("PerformanceAnalytics")
library(dynamicTreeCut)
library(xcms)
library(RColorBrewer)
library(ggrepel)
library(IRanges)
library(MASS)
library(dbscan)
```

```{r setup, include=FALSE, cache=FALSE}
## This chunk should contain global configuration commands.
## Use this to set knitr options and related things. Everything
## in this chunk will be included in an appendix to document the
## configuration used.
#output <- opts_knit$get("rmarkdown.pandoc.to")

## By default R code is only included in HTML versions of the report
## (where it can be collapsed). You can generate a PDF version
## using rmarkdown::pdf_document to get a copy for print. Extensive
## chunks of R code may or may not be desired in /hat setting. If you
## want them simply change the following arguments to `echo = TRUE`.
## In either case the default can be overwritten for individual chunks.
#opts_chunk$set(echo = output=="html")
#opts_chunk$set(warning = output=="html")
#opts_chunk$set(message = output=="html")

## Cache options
opts_chunk$set(cache=TRUE)

## Figure options
## Set default figure format
#options(reportmd.figure.format=params$format)

## Set 'hide.fig.code' to FALSE to include code chunks that
## produce Figures in the output. Note that this affects all chunks
## that provide a figure caption.
opts_chunk$set(hold=TRUE, hide.fig.code=FALSE)

## Set up default plotting options for different formats.
## These can be overwritten for individual chunks
#interactiveFig()
#screenFig()
#printFig()

## Pander options
panderOptions("digits", 3)
panderOptions("table.split.table", 160)

## Configure Figure and Table lables
#options(figcap.prefix = "Figure", figcap.sep = ":", figcap.prefix.highlight = "**")
#options(tabcap.prefix = "Table", tabcap.sep = ":", tabcap.prefix.highlight = "**")

## Install required knitr hooks
#installHooks()
```

```{r functions, include=FALSE}
## Custom functions used in the analysis should go into this chunk.
## They will be listed in their own section of the appendix.
makePeaks<-function(.xcms){
  # this function converts XCMSRaw data into mz table
    sel <- profRange(.xcms)
    peaks<-data.frame(mz=100.0,
                      intensity=1.0e6,
                      ion=NA,
                      MP=NA,
                      time=0.0,
                      scan=1,
                      delta=NA,
                      adduct=NA)[FALSE,]
    l<-list()
    l<-lapply(sel$scanidx,function(i){
        scan <- as.data.table(getScan(xraw, sel$scanidx[i], sel$mzrange))
        p<-scan
        p$ion<-NA
        p$time<-xraw@scantime[i]
        p$scan<-i
        return(p)
    })
    peaks<-as.data.table(ldply(l,rbind))
    return(peaks)
}
getScanPlot<-function(mz1){
  mzp<-mz1
  mzp[,lab:=paste(round(mz,4))]
  mzp[order(intensity,decreasing = TRUE)[-c(1:10)],lab:='']
  p<-ggplot(mzp, aes(x=mz,yend=0,xend=mz, y=intensity)) +
    geom_segment()+geom_point(size=0.15) + #scale_y_log10()+
    geom_text_repel(aes(x = mz,y=intensity,label=lab))+
    theme(legend.position="none")
  return(p)
}

# Multiple plot function
#
# ggplot objects can be passed in ..., or to plotlist (as a list of ggplot objects)
# - cols:   Number of columns in layout
# - layout: A matrix specifying the layout. If present, 'cols' is ignored.
#
# If the layout is something like matrix(c(1,2,3,3), nrow=2, byrow=TRUE),
# then plot 1 will go in the upper left, 2 will go in the upper right, and
# 3 will go all the way across the bottom.
# 
# Code is taken from http://www.cookbook-r.com/Graphs/Multiple_graphs_on_one_page_%28ggplot2%29/
#
multiplot <- function(..., plotlist=NULL, file, cols=1, layout=NULL) {
  library(grid)

  # Make a list from the ... arguments and plotlist
  plots <- c(list(...), plotlist)

  numPlots = length(plots)

  # If layout is NULL, then use 'cols' to determine layout
  if (is.null(layout)) {
    # Make the panel
    # ncol: Number of columns of plots
    # nrow: Number of rows needed, calculated from # of cols
    layout <- matrix(seq(1, cols * ceiling(numPlots/cols)),
                    ncol = cols, nrow = ceiling(numPlots/cols))
  }

 if (numPlots==1) {
    print(plots[[1]])

  } else {
    # Set up the page
    grid.newpage()
    pushViewport(viewport(layout = grid.layout(nrow(layout), ncol(layout))))

    # Make each plot, in the correct location
    for (i in 1:numPlots) {
      # Get the i,j matrix positions of the regions that contain this subplot
      matchidx <- as.data.frame(which(layout == i, arr.ind = TRUE))

      print(plots[[i]], vp = viewport(layout.pos.row = matchidx$row,
                                      layout.pos.col = matchidx$col))
    }
  }
}

get_Rle<-function(.scan,.max){
    require(IRanges)
  xf <- rep(0, .max)
  xf[.scan] <- 1
  Rle(xf) -> r
  return(r)
}

get_runLen <- function(.scan, .max) {
  r<-get_Rle(.scan,.max)
  return(runLength(r)[which(runValue(r)==1)])
}

getFeature<-function(l,p.,th=2.0){
  range(l$residuals[abs(rstandard(l)) <= 2]) -> bnd
  pl <- predict(l, p.)
  rl <- p.$mz - pl
  idL <- which(rl >= bnd[1] & rl <= bnd[2])
  return(idL)
}
print_tableP<-function(mat,digits=-5){
  addtorow          <- list()
  addtorow$pos      <- list()
  addtorow$pos[[1]] <- c(0)
  addtorow$command  <- c(
    paste(
      "\\hline \n",
      "\\endhead \n",
      "\\hline \n",
      "\\multicolumn{3}{l}{\\footnotesize Continued on next page} \n",
      "\\endfoot \n",
      "\\endlastfoot \n",sep = ""
    )
  )
  cat(
    sprintf(
      "\\begin{center}\n\\captionof{table}{Wide ranges of continious peaks (width \textgreater %d)}\n\\scriptsize",50
    )
  )
  print(
    xtable(
      mat,digits=digits)
    ,size = "small",include.colnames = TRUE,
    tabular.environment = "longtable",
    floating = FALSE,include.rownames = TRUE,
    add.to.row = addtorow,
    hline.after =c(-1)
  )
  cat("\\end{center}\n")
}
```

```{r set.constants,include=FALSE}
  FName<-'0_00_HealthyBrain_0_FT100k.raw.mzXML'
  path<-'/Users/lptolik/Yandex.Disk.localized/BDmzXML/FT_centroid/'
  ppm<-50
  binwidth<-0.1
  .pal <- brewer.pal(12,'Paired')
  .minPts<-20
```

# Load the data

Thermo RAW files were converted into mzXML by ReAdW.201510.xcalibur.exe 
application. After that we load mzXML file into R with help of XCMS package and
convert it into MZ table:
```{r read.file, cache=TRUE}
  xraw <- xcmsRaw(paste0(path,FName))
  p<-makePeaks(xraw)
  scanLen <- max(p$scan)
  load('models.RData')
```

As a result we have table with 5 columns: `r paste0('"',names(p),'"')` to store 
m/z, intensity, time of scan and its number. Column "ion" is reserved for ID of
the feature, obtained with further analysis.

# Feature selection

The algorithm process data in the MZ table in intensity descending order, so 
whenever it stops we can be sure that we identify most important features in the
data. 

At the first step algorithm selects row with highest intensity, which is not 
assigned to any feature yet, and take its m/z value as proposed feature position:

```{r get.mz}
  idx<-which(is.na(p$ion))
  cP<-which.max(p$intensity[idx])
  ionI<-idx[cP]
  maxMZ<-p$mz[ionI]
```

The selected row represents the peak with m/z=`r round(maxMZ,4)` of the scan 
N=`r p$scan[ionI]` taken at time `r round(p$time[ionI],1)` sec: 

```{r ion.plots,fig.width=8.5,fig.height=4.5,results='asis',cache=FALSE}
print(getScanPlot(p[scan==p$scan[ionI]]))
```

Now we collect all signals in the vicinity (50 ppm) of the selected m/z value and analyze
how they drift with time:

```{r get.subset}
  idx <- which(abs(p$mz - maxMZ) < (ppm * 1e-6 * maxMZ))
  p. <- p[idx, ]
```

To separate feature from surrounding noise we cluster data in m/z domain by 
hierarchical Ward clustering and select the most stable clusters with 
dynamicTreeCut:

```{r cluster.mz}
  dissim1 = dist(p.$mz)
  dendro1 <- hclust(d = dissim1, method = 'ward.D2')
  ct1 <- cutreeDynamic(
    dendro1,
    cutHeight = NULL,
    minClusterSize = 30,
    method = "hybrid",
    deepSplit = 0,
    pamStage = TRUE,
    distM = as.matrix(dissim1),
    maxPamDist = 0,
    verbose = 0
  )
  cct1 <- ct1[which.max(p.$intensity)]
  id.1 <- idx[ct1 == cct1]
  mz1 <- median(p$mz[id.1])
  p.$color<-.pal[ct1+1]
  rl <- get_runLen(p$scan[id.1], scanLen)
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),log='y',color=color)
  p2<-qplot(time,mz,data = p.,color=color)
  p3<-qplot(mz,intensity,data = p.,color=color)
  print(multiplot(p1, p2, p3, cols=1))
  
```

It could be seen that selected m/z value forms a well separated cluster as in m/z 
as in intensity domain, but this is not always the case. There are peaks which 
could not be separated by such procedure, for example:

It could be seen that even in this case each feature visually represents well 
separated line on the time vs m/z plot. So we can extract distinct features by 
fitting data from cluster containing peak of highest intensity to the linear 
function:

```{r fit.lin,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE}
  l<-lm(mz ~ time, data = p[id.1, ])
if (all(!is.na(l$coefficients))) {
  op <- par(mfrow = c(2, 2), oma = c(0, 0, 2, 0))
  plot(l)
  par(op)
}
```

Now to incorporate to the feature all points, that form the line even if they 
appear in different cluster we predict linear value for each point in the 
selected interval and filter out all points with residuals higher than two 
standard deviations:
```{r filter.lm}
if (all(!is.na(l$coefficients))) {
  range(l$residuals[abs(rstandard(l)) <= 2]) -> bnd
  pl <- predict(l, p.)
  rl <- p.$mz - pl
  idL <- which(rl >= bnd[1] & rl <= bnd[2])
  p5 = qplot(
          time,
          mz,
          data = p.[idL],
          color = color,
          xlim = range(p.$time),
          ylim = range(p.$mz)
  )
  p6 = qplot(time,
          mz,
          data = p.,
          color = color) +
      geom_abline(intercept = l$coefficients[1],
          slope = l$coefficients[2])
  
  p.$resPPM<-rl*1e6/l$coefficients[1]
  p7<-qplot(resPPM,data=p.[idL],
            binwidth=binwidth,color=color)+
    stat_function(fun = function(x, mean, sd, n, bw){
      dnorm(x = x, mean = mean, sd = sd) * n * bw
      },
      args = c(mean = mean(p.$resPPM[idL]), 
               sd = sd(p.$resPPM[idL]), 
               n = length(idL), 
               bw = binwidth))

  print(multiplot(p5, p6, p7, cols = 1))
}
```

The fitted line will change due to rearrangement of the feature point list, so 
we will repeat this step untill it converges or until range of residuals less 
than 1 ppm, but no more than 5 times.

Final step of the algorithm is the quality control. The feature could be too wide
in m/z domain or it could eventually consists of several close crests, it could
contains several points from the same scan. 
As we looking for continious features and rely on linear regression we will 
remove from the list all features which is too short to produse reliable 
regression. Only features wich contains more than 300 points in of more than 
100 consequtive scans will be preserved. High intensity bursting ions should be
analysed separately.

```{r get.quality}
  qualityTab<-data.frame(ion=maxMZ,
                         medOutlier=NA,
                         numClass=NA,
                         numZero=NA,
                         N=NA,
                         ppmSD=NA,
                         ppmRange=NA,
                         ppmIQR=NA)
  i<-1
  ct2 <- hdbscan(scale(p.[,c('mz')]), minPts = min(.minPts,dim(p.)[1]/4))
  p.$color<-.pal[ct2$cluster+1]
  p.$sz<-ct2$membership_prob
  qualityTab$medOutlier[i]<-median(ct2$outlier_scores,na.rm = TRUE)
  qualityTab$numClass[i]<-max(ct2$cluster)
  qualityTab$numZero[i]<-length(which(ct2$cluster==0))
  qualityTab$N[i]<-dim(p.)[1]
  qualityTab$ppmSD[i]<-1e6*sd(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$ppmRange[i]<-1e6*diff(range(p.$mz))/maxMZ
  qualityTab$ppmIQR[i]<-1e6*IQR(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$logOutlier<-log10(qualityTab$medOutlier)
  qualityTab$Range2SD<-qualityTab$ppmRange/qualityTab$ppmSD
  qualityTab$SD2IQR<-qualityTab$ppmSD/qualityTab$ppmIQR
  predict(model2, newdata = (qualityTab[,-1]))->pred0
  summary(pred0)
```

The model has two type of output: 'g' for accepted features and 'b' for rejected.
The result of our feature is 'g', so it is accepted.


# Accepted continious features
## MZ=845.6437
```{r get.mz.845,echo=FALSE}
  idx<-which(abs(p$mz-845.6437)<1e-3)
  cP<-which.max(p$intensity[idx])
  ionI<-idx[cP]
  maxMZ<-p$mz[ionI]
```
```{r get.subset.845,echo=FALSE}
  idx <- which(abs(p$mz - maxMZ) < (ppm * 1e-6 * maxMZ))
  p. <- p[idx, ]
```
```{r cluster.mz.845,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  dissim1 = dist(p.$mz)
  dendro1 <- hclust(d = dissim1, method = 'ward.D2')
  ct1 <- cutreeDynamic(
    dendro1,
    cutHeight = NULL,
    minClusterSize = 30,
    method = "hybrid",
    deepSplit = 0,
    pamStage = TRUE,
    distM = as.matrix(dissim1),
    maxPamDist = 0,
    verbose = 0
  )
  cct1 <- ct1[which.min(abs(p.$mz - maxMZ))]
  id.1 <- idx[ct1 == cct1]
  mz1 <- median(p$mz[id.1])
  p.$color<-.pal[ct1+1]
  rl <- get_runLen(p$scan[id.1], scanLen)
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),log='y',color=color)
  p2<-qplot(time,mz,data = p.,color=color)
  p3<-qplot(mz,intensity,data = p.,color=color)
  print(multiplot(p1, p2, p3, cols=1))
  
```


```{r fit.lin.845,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  l<-lm(mz ~ time, data = p[id.1, ])
if (FALSE){#all(!is.na(l$coefficients))) {
  op <- par(mfrow = c(2, 2), oma = c(0, 0, 2, 0))
  plot(l)
  par(op)
}
```

```{r filter.lm.845,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
if (all(!is.na(l$coefficients))) {
  range(l$residuals[abs(rstandard(l)) <= 2]) -> bnd
  pl <- predict(l, p.)
  rl <- p.$mz - pl
  idL <- which(rl >= bnd[1] & rl <= bnd[2])
  p5 = qplot(
          time,
          mz,
          data = p.[idL],
          color = color,
          xlim = range(p.$time),
          ylim = range(p.$mz)
  )
  p6 = qplot(time,
          mz,
          data = p.,
          color = color) +
      geom_abline(intercept = l$coefficients[1],
          slope = l$coefficients[2])
  
  p.$resPPM<-rl*1e6/l$coefficients[1]
  p7<-qplot(resPPM,data=p.[idL],
            binwidth=binwidth,color=color)+
    stat_function(fun = function(x, mean, sd, n, bw){
      dnorm(x = x, mean = mean, sd = sd) * n * bw
      },
      args = c(mean = mean(p.$resPPM[idL]), 
               sd = sd(p.$resPPM[idL]), 
               n = length(idL), 
               bw = binwidth))
  print(multiplot(p6, p7, cols = 1))
}
```

```{r get.quality.845,echo=FALSE,eval=FALSE}
  qualityTab<-data.frame(ion=maxMZ,
                         medOutlier=NA,
                         numClass=NA,
                         numZero=NA,
                         N=NA,
                         ppmSD=NA,
                         ppmRange=NA,
                         ppmIQR=NA)
  i<-1
  ct2 <- hdbscan(scale(p.[,c('mz')]), minPts = min(.minPts,dim(p.)[1]/4))
  p.$color<-.pal[ct2$cluster+1]
  p.$sz<-ct2$membership_prob
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),color=color,alpha=sz,log='y')
  p2<-qplot(time,mz,data = p.,color=color,alpha=sz)
  p3<-qplot(mz,intensity,data = p.,color=color,alpha=sz)
  p4<-qplot(mz,ion,data=p.,color=color,alpha=sz)
  #print(multiplot(p1, p2, p3,p4, cols=1))
  #qplot(mz,intensity,data = p.,facets =factor(ct2$cluster)~.,color=color,alpha=sz)
  qualityTab$medOutlier[i]<-median(ct2$outlier_scores,na.rm = TRUE)
  qualityTab$numClass[i]<-max(ct2$cluster)
  qualityTab$numZero[i]<-length(which(ct2$cluster==0))
  qualityTab$N[i]<-dim(p.)[1]
  qualityTab$ppmSD[i]<-1e6*sd(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$ppmRange[i]<-1e6*diff(range(p.$mz))/maxMZ
  qualityTab$ppmIQR[i]<-1e6*IQR(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$logOutlier<-log10(qualityTab$medOutlier)
  qualityTab$Range2SD<-qualityTab$ppmRange/qualityTab$ppmSD
  qualityTab$SD2IQR<-qualityTab$ppmSD/qualityTab$ppmIQR
  predict(model2, newdata = (qualityTab[,-1]))->pred0
  summary(pred0)
```




## MZ=846.6437
```{r get.mz.846,echo=FALSE}
  idx<-which(abs(p$mz-846.6437)<1e-2)
  cP<-which.max(p$intensity[idx])
  ionI<-idx[cP]
  maxMZ<-p$mz[ionI]
```

```{r get.subset.846,echo=FALSE}
  idx <- which(abs(p$mz - maxMZ) < (ppm * 1e-6 * maxMZ))
  p. <- p[idx, ]
```

```{r cluster.mz.846,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  dissim1 = dist(p.$mz)
  dendro1 <- hclust(d = dissim1, method = 'ward.D2')
  ct1 <- cutreeDynamic(
    dendro1,
    cutHeight = NULL,
    minClusterSize = 30,
    method = "hybrid",
    deepSplit = 0,
    pamStage = TRUE,
    distM = as.matrix(dissim1),
    maxPamDist = 0,
    verbose = 0
  )
  cct1 <- ct1[which.min(abs(p.$mz - maxMZ))]
  id.1 <- idx[ct1 == cct1]
  mz1 <- median(p$mz[id.1])
  p.$color<-.pal[ct1+1]
  rl <- get_runLen(p$scan[id.1], scanLen)
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),log='y',color=color)
  p2<-qplot(time,mz,data = p.,color=color)
  p3<-qplot(mz,intensity,data = p.,color=color)
  print(multiplot(p1, p2, p3, cols=1))
  
```


```{r fit.lin.846,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  l<-lm(mz ~ time, data = p[id.1, ])
if (FALSE){#all(!is.na(l$coefficients))) {
  op <- par(mfrow = c(2, 2), oma = c(0, 0, 2, 0))
  plot(l)
  par(op)
}
```

```{r filter.lm.846,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
if (all(!is.na(l$coefficients))) {
  range(l$residuals[abs(rstandard(l)) <= 2]) -> bnd
  pl <- predict(l, p.)
  rl <- p.$mz - pl
  idL <- which(rl >= bnd[1] & rl <= bnd[2])
  p5 = qplot(
          time,
          mz,
          data = p.[idL],
          color = color,
          xlim = range(p.$time),
          ylim = range(p.$mz)
  )
  p6 = qplot(time,
          mz,
          data = p.,
          color = color) +
      geom_abline(intercept = l$coefficients[1],
          slope = l$coefficients[2])
  
  p.$resPPM<-rl*1e6/l$coefficients[1]
  p7<-qplot(resPPM,data=p.[idL],
            binwidth=binwidth,color=color)+
    stat_function(fun = function(x, mean, sd, n, bw){
      dnorm(x = x, mean = mean, sd = sd) * n * bw
      },
      args = c(mean = mean(p.$resPPM[idL]), 
               sd = sd(p.$resPPM[idL]), 
               n = length(idL), 
               bw = binwidth))
  print(multiplot(p5, p6, p7, cols = 1))
}
```


```{r get.quality.846,echo=FALSE,eval=FALSE}
  qualityTab<-data.frame(ion=maxMZ,
                         medOutlier=NA,
                         numClass=NA,
                         numZero=NA,
                         N=NA,
                         ppmSD=NA,
                         ppmRange=NA,
                         ppmIQR=NA)
  i<-1
  ct2 <- hdbscan(scale(p.[,c('mz')]), minPts = min(.minPts,dim(p.)[1]/4))
  p.$color<-.pal[ct2$cluster+1]
  p.$sz<-ct2$membership_prob
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),color=color,alpha=sz,log='y')
  p2<-qplot(time,mz,data = p.,color=color,alpha=sz)
  p3<-qplot(mz,intensity,data = p.,color=color,alpha=sz)
  p4<-qplot(mz,ion,data=p.,color=color,alpha=sz)
  print(multiplot(p1, p2, p3,p4, cols=1))
  qplot(mz,intensity,data = p.,facets =factor(ct2$cluster)~.,color=color,alpha=sz)
  qualityTab$medOutlier[i]<-median(ct2$outlier_scores,na.rm = TRUE)
  qualityTab$numClass[i]<-max(ct2$cluster)
  qualityTab$numZero[i]<-length(which(ct2$cluster==0))
  qualityTab$N[i]<-dim(p.)[1]
  qualityTab$ppmSD[i]<-1e6*sd(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$ppmRange[i]<-1e6*diff(range(p.$mz))/maxMZ
  qualityTab$ppmIQR[i]<-1e6*IQR(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$logOutlier<-log10(qualityTab$medOutlier)
  qualityTab$Range2SD<-qualityTab$ppmRange/qualityTab$ppmSD
  qualityTab$SD2IQR<-qualityTab$ppmSD/qualityTab$ppmIQR
  predict(model2, newdata = (qualityTab[,-1]))->pred0
  summary(pred0)
  table(pred0)
```



## MZ=651.477226
```{r get.mz.651,echo=FALSE}
  idx<-which(abs(p$mz-651.477226)<1e-3)
  cP<-which.max(p$intensity[idx])
  ionI<-idx[cP]
  maxMZ<-p$mz[ionI]
```

```{r get.subset.651,echo=FALSE}
  idx <- which(abs(p$mz - maxMZ) < (ppm * 1e-6 * maxMZ))
  p. <- p[idx, ]
```

```{r cluster.mz.651,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  dissim1 = dist(p.$mz)
  dendro1 <- hclust(d = dissim1, method = 'ward.D2')
  ct1 <- cutreeDynamic(
    dendro1,
    cutHeight = NULL,
    minClusterSize = 30,
    method = "hybrid",
    deepSplit = 0,
    pamStage = TRUE,
    distM = as.matrix(dissim1),
    maxPamDist = 0,
    verbose = 0
  )
  cct1 <- ct1[which.min(abs(p.$mz - maxMZ))]
  id.1 <- idx[ct1 == cct1]
  mz1 <- median(p$mz[id.1])
  p.$color<-.pal[ct1+1]
  rl <- get_runLen(p$scan[id.1], scanLen)
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),log='y',color=color)
  p2<-qplot(time,mz,data = p.,color=color)
  p3<-qplot(mz,intensity,data = p.,color=color)
  print(multiplot(p1, p2, p3, cols=1))
  
```


```{r fit.lin.651,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  l<-lm(mz ~ time, data = p[id.1, ])
if (FALSE){#all(!is.na(l$coefficients))) {
  op <- par(mfrow = c(2, 2), oma = c(0, 0, 2, 0))
  plot(l)
  par(op)
}
```

```{r filter.lm.651,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
if (all(!is.na(l$coefficients))) {
  range(l$residuals[abs(rstandard(l)) <= 2]) -> bnd
  pl <- predict(l, p.)
  rl <- p.$mz - pl
  idL <- which(rl >= bnd[1] & rl <= bnd[2])
  p5 = qplot(
          time,
          mz,
          data = p.[idL],
          color = color,
          xlim = range(p.$time),
          ylim = range(p.$mz)
  )
  p6 = qplot(time,
          mz,
          data = p.,
          color = color) +
      geom_abline(intercept = l$coefficients[1],
          slope = l$coefficients[2])
  
  p.$resPPM<-rl*1e6/l$coefficients[1]
  p7<-qplot(resPPM,data=p.[idL],
            binwidth=binwidth,color=color)+
    stat_function(fun = function(x, mean, sd, n, bw){
      dnorm(x = x, mean = mean, sd = sd) * n * bw
      },
      args = c(mean = mean(p.$resPPM[idL]), 
               sd = sd(p.$resPPM[idL]), 
               n = length(idL), 
               bw = binwidth))

  print(multiplot(p5, p6, p7, cols = 1))
}
```

```{r get.quality.651,echo=FALSE,eval=FALSE}
  qualityTab<-data.frame(ion=maxMZ,
                         medOutlier=NA,
                         numClass=NA,
                         numZero=NA,
                         N=NA,
                         ppmSD=NA,
                         ppmRange=NA,
                         ppmIQR=NA)
  i<-1
  ct2 <- hdbscan(scale(p.[,c('mz')]), minPts = min(.minPts,dim(p.)[1]/4))
  p.$color<-.pal[ct2$cluster+1]
  p.$sz<-ct2$membership_prob
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),color=color,alpha=sz,log='y')
  p2<-qplot(time,mz,data = p.,color=color,alpha=sz)
  p3<-qplot(mz,intensity,data = p.,color=color,alpha=sz)
  p4<-qplot(mz,ion,data=p.,color=color,alpha=sz)
  print(multiplot(p1, p2, p3,p4, cols=1))
  qplot(mz,intensity,data = p.,facets =factor(ct2$cluster)~.,color=color,alpha=sz)
  qualityTab$medOutlier[i]<-median(ct2$outlier_scores,na.rm = TRUE)
  qualityTab$numClass[i]<-max(ct2$cluster)
  qualityTab$numZero[i]<-length(which(ct2$cluster==0))
  qualityTab$N[i]<-dim(p.)[1]
  qualityTab$ppmSD[i]<-1e6*sd(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$ppmRange[i]<-1e6*diff(range(p.$mz))/maxMZ
  qualityTab$ppmIQR[i]<-1e6*IQR(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$logOutlier<-log10(qualityTab$medOutlier)
  qualityTab$Range2SD<-qualityTab$ppmRange/qualityTab$ppmSD
  qualityTab$SD2IQR<-qualityTab$ppmSD/qualityTab$ppmIQR
  predict(model2, newdata = (qualityTab[,-1]))->pred0
  summary(pred0)
  table(pred0)
```


## MZ=600.513493
```{r get.mz.600,echo=FALSE}
  idx<-which(abs(p$mz-600.513493)<1e-3)
  cP<-which.max(p$intensity[idx])
  ionI<-idx[cP]
  maxMZ<-p$mz[ionI]
```


```{r get.subset.600,echo=FALSE}
  idx <- which(abs(p$mz - maxMZ) < (ppm * 1e-6 * maxMZ))
  p. <- p[idx, ]
```

```{r cluster.mz.600,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  dissim1 = dist(p.$mz)
  dendro1 <- hclust(d = dissim1, method = 'ward.D2')
  ct1 <- cutreeDynamic(
    dendro1,
    cutHeight = NULL,
    minClusterSize = 30,
    method = "hybrid",
    deepSplit = 0,
    pamStage = TRUE,
    distM = as.matrix(dissim1),
    maxPamDist = 0,
    verbose = 0
  )
  cct1 <- ct1[which.min(abs(p.$mz - maxMZ))]
  id.1 <- idx[ct1 == cct1]
  mz1 <- median(p$mz[id.1])
  p.$color<-.pal[ct1+1]
  rl <- get_runLen(p$scan[id.1], scanLen)
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),log='y',color=color)
  p2<-qplot(time,mz,data = p.,color=color)
  p3<-qplot(mz,intensity,data = p.,color=color)
  print(multiplot(p1, p2, p3, cols=1))
  
```


```{r fit.lin.600,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  l<-lm(mz ~ time, data = p[id.1, ])
if (FALSE){#all(!is.na(l$coefficients))) {
  op <- par(mfrow = c(2, 2), oma = c(0, 0, 2, 0))
  plot(l)
  par(op)
}
```

```{r filter.lm.600,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
if (all(!is.na(l$coefficients))) {
  range(l$residuals[abs(rstandard(l)) <= 2]) -> bnd
  pl <- predict(l, p.)
  rl <- p.$mz - pl
  idL <- which(rl >= bnd[1] & rl <= bnd[2])
  p5 = qplot(
          time,
          mz,
          data = p.[idL],
          color = color,
          xlim = range(p.$time),
          ylim = range(p.$mz)
  )
  p6 = qplot(time,
          mz,
          data = p.,
          color = color) +
      geom_abline(intercept = l$coefficients[1],
          slope = l$coefficients[2])
  
  p.$resPPM<-rl*1e6/l$coefficients[1]
  p7<-qplot(resPPM,data=p.[idL],
            binwidth=binwidth,color=color)+
    stat_function(fun = function(x, mean, sd, n, bw){
      dnorm(x = x, mean = mean, sd = sd) * n * bw
      },
      args = c(mean = mean(p.$resPPM[idL]), 
               sd = sd(p.$resPPM[idL]), 
               n = length(idL), 
               bw = binwidth))
  print(multiplot(p5, p6, p7, cols = 1))
}
```

```{r get.quality.600,echo=FALSE,eval=FALSE}
  qualityTab<-data.frame(ion=maxMZ,
                         medOutlier=NA,
                         numClass=NA,
                         numZero=NA,
                         N=NA,
                         ppmSD=NA,
                         ppmRange=NA,
                         ppmIQR=NA)
  i<-1
  ct2 <- hdbscan(scale(p.[,c('mz')]), minPts = min(.minPts,dim(p.)[1]/4))
  p.$color<-.pal[ct2$cluster+1]
  p.$sz<-ct2$membership_prob
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),color=color,alpha=sz,log='y')
  p2<-qplot(time,mz,data = p.,color=color,alpha=sz)
  p3<-qplot(mz,intensity,data = p.,color=color,alpha=sz)
  p4<-qplot(mz,ion,data=p.,color=color,alpha=sz)
  print(multiplot(p1, p2, p3,p4, cols=1))
  qplot(mz,intensity,data = p.,facets =factor(ct2$cluster)~.,color=color,alpha=sz)
  qualityTab$medOutlier[i]<-median(ct2$outlier_scores,na.rm = TRUE)
  qualityTab$numClass[i]<-max(ct2$cluster)
  qualityTab$numZero[i]<-length(which(ct2$cluster==0))
  qualityTab$N[i]<-dim(p.)[1]
  qualityTab$ppmSD[i]<-1e6*sd(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$ppmRange[i]<-1e6*diff(range(p.$mz))/maxMZ
  qualityTab$ppmIQR[i]<-1e6*IQR(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$logOutlier<-log10(qualityTab$medOutlier)
  qualityTab$Range2SD<-qualityTab$ppmRange/qualityTab$ppmSD
  qualityTab$SD2IQR<-qualityTab$ppmSD/qualityTab$ppmIQR
  predict(model2, newdata = (qualityTab[,-1]))->pred0
  summary(pred0)
  table(pred0)
```



## MZ=602.510830
```{r get.mz.602,echo=FALSE}
  idx<-which(abs(p$mz-602.510830)<1e-3)
  cP<-which.max(p$intensity[idx])
  ionI<-idx[cP]
  maxMZ<-p$mz[ionI]
```


```{r get.subset.602,echo=FALSE}
  idx <- which(abs(p$mz - maxMZ) < (ppm * 1e-6 * maxMZ))
  p. <- p[idx, ]
```


```{r cluster.mz.602,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  dissim1 = dist(p.$mz)
  dendro1 <- hclust(d = dissim1, method = 'ward.D2')
  ct1 <- cutreeDynamic(
    dendro1,
    cutHeight = NULL,
    minClusterSize = 30,
    method = "hybrid",
    deepSplit = 0,
    pamStage = TRUE,
    distM = as.matrix(dissim1),
    maxPamDist = 0,
    verbose = 0
  )
  cct1 <- ct1[which.min(abs(p.$mz - maxMZ))]
  id.1 <- idx[ct1 == cct1]
  mz1 <- median(p$mz[id.1])
  p.$color<-.pal[ct1+1]
  rl <- get_runLen(p$scan[id.1], scanLen)
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),log='y',color=color)
  p2<-qplot(time,mz,data = p.,color=color)
  p3<-qplot(mz,intensity,data = p.,color=color)
  print(multiplot(p1, p2, p3, cols=1))
  
```


```{r fit.lin.602,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  l<-lm(mz ~ time, data = p[id.1, ])
if (FALSE){#all(!is.na(l$coefficients))) {
  op <- par(mfrow = c(2, 2), oma = c(0, 0, 2, 0))
  plot(l)
  par(op)
}
```

```{r filter.lm.602,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
if (all(!is.na(l$coefficients))) {
  range(l$residuals[abs(rstandard(l)) <= 2]) -> bnd
  pl <- predict(l, p.)
  rl <- p.$mz - pl
  idL <- which(rl >= bnd[1] & rl <= bnd[2])
  p5 = qplot(
          time,
          mz,
          data = p.[idL],
          color = color,
          xlim = range(p.$time),
          ylim = range(p.$mz)
  )
  p6 = qplot(time,
          mz,
          data = p.,
          color = color) +
      geom_abline(intercept = l$coefficients[1],
          slope = l$coefficients[2])
  
  p.$resPPM<-rl*1e6/l$coefficients[1]
  p7<-qplot(resPPM,data=p.[idL],
            binwidth=binwidth,color=color)+
    stat_function(fun = function(x, mean, sd, n, bw){
      dnorm(x = x, mean = mean, sd = sd) * n * bw
      },
      args = c(mean = mean(p.$resPPM[idL]), 
               sd = sd(p.$resPPM[idL]), 
               n = length(idL), 
               bw = binwidth))

  print(multiplot(p5, p6, p7, cols = 1))
}
```

```{r get.quality.602,echo=FALSE,eval=FALSE}
  qualityTab<-data.frame(ion=maxMZ,
                         medOutlier=NA,
                         numClass=NA,
                         numZero=NA,
                         N=NA,
                         ppmSD=NA,
                         ppmRange=NA,
                         ppmIQR=NA)
  i<-1
  ct2 <- hdbscan(scale(p.[,c('mz')]), minPts = min(.minPts,dim(p.)[1]/4))
  p.$color<-.pal[ct2$cluster+1]
  p.$sz<-ct2$membership_prob
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),color=color,alpha=sz,log='y')
  p2<-qplot(time,mz,data = p.,color=color,alpha=sz)
  p3<-qplot(mz,intensity,data = p.,color=color,alpha=sz)
  p4<-qplot(mz,ion,data=p.,color=color,alpha=sz)
  print(multiplot(p1, p2, p3,p4, cols=1))
  qplot(mz,intensity,data = p.,facets =factor(ct2$cluster)~.,color=color,alpha=sz)
  qualityTab$medOutlier[i]<-median(ct2$outlier_scores,na.rm = TRUE)
  qualityTab$numClass[i]<-max(ct2$cluster)
  qualityTab$numZero[i]<-length(which(ct2$cluster==0))
  qualityTab$N[i]<-dim(p.)[1]
  qualityTab$ppmSD[i]<-1e6*sd(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$ppmRange[i]<-1e6*diff(range(p.$mz))/maxMZ
  qualityTab$ppmIQR[i]<-1e6*IQR(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$logOutlier<-log10(qualityTab$medOutlier)
  qualityTab$Range2SD<-qualityTab$ppmRange/qualityTab$ppmSD
  qualityTab$SD2IQR<-qualityTab$ppmSD/qualityTab$ppmIQR
  predict(model2, newdata = (qualityTab[,-1]))->pred0
  summary(pred0)
  table(pred0)
```



## MZ=794.5708
```{r get.mz.794,echo=FALSE}
  idx<-which(abs(p$mz-794.5708)<1e-3)
  cP<-which.max(p$intensity[idx])
  ionI<-idx[cP]
  maxMZ<-p$mz[ionI]
```

```{r get.subset.794,echo=FALSE}
  idx <- which(abs(p$mz - maxMZ) < (ppm * 1e-6 * maxMZ))
  p. <- p[idx, ]
```

```{r cluster.mz.794,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  dissim1 = dist(p.$mz)
  dendro1 <- hclust(d = dissim1, method = 'ward.D2')
  ct1 <- cutreeDynamic(
    dendro1,
    cutHeight = NULL,
    minClusterSize = 30,
    method = "hybrid",
    deepSplit = 0,
    pamStage = TRUE,
    distM = as.matrix(dissim1),
    maxPamDist = 0,
    verbose = 0
  )
  cct1 <- ct1[which.min(abs(p.$mz - maxMZ))]
  id.1 <- idx[ct1 == cct1]
  mz1 <- median(p$mz[id.1])
  p.$color<-.pal[ct1+1]
  rl <- get_runLen(p$scan[id.1], scanLen)
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),log='y',color=color)
  p2<-qplot(time,mz,data = p.,color=color)
  p3<-qplot(mz,intensity,data = p.,color=color)
  print(multiplot(p1, p2, p3, cols=1))
  
```


```{r fit.lin.794,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  l<-lm(mz ~ time, data = p[id.1, ])
if (all(!is.na(l$coefficients))) {
  op <- par(mfrow = c(2, 2), oma = c(0, 0, 2, 0))
  plot(l)
  par(op)
}
```

```{r filter.lm.794,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
if (all(!is.na(l$coefficients))) {
  range(l$residuals[abs(rstandard(l)) <= 2]) -> bnd
  pl <- predict(l, p.)
  rl <- p.$mz - pl
  idL <- which(rl >= bnd[1] & rl <= bnd[2])
  p5 = qplot(
          time,
          mz,
          data = p.[idL],
          color = color,
          xlim = range(p.$time),
          ylim = range(p.$mz)
  )
  p6 = qplot(time,
          mz,
          data = p.,
          color = color) +
      geom_abline(intercept = l$coefficients[1],
          slope = l$coefficients[2])
  
  p.$resPPM<-rl*1e6/l$coefficients[1]
  p7<-qplot(resPPM,data=p.[idL],
            binwidth=binwidth,color=color)+
    stat_function(fun = function(x, mean, sd, n, bw){
      dnorm(x = x, mean = mean, sd = sd) * n * bw
      },
      args = c(mean = mean(p.$resPPM[idL]), 
               sd = sd(p.$resPPM[idL]), 
               n = length(idL), 
               bw = binwidth))
  print(multiplot(p5, p6, p7, cols = 1))
}
```
```{r get.quality.794,echo=FALSE,eval=FALSE}
  qualityTab<-data.frame(ion=maxMZ,
                         medOutlier=NA,
                         numClass=NA,
                         numZero=NA,
                         N=NA,
                         ppmSD=NA,
                         ppmRange=NA,
                         ppmIQR=NA)
  i<-1
  ct2 <- hdbscan(scale(p.[,c('mz')]), minPts = min(.minPts,dim(p.)[1]/4))
  p.$color<-.pal[ct2$cluster+1]
  p.$sz<-ct2$membership_prob
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),color=color,alpha=sz,log='y')
  p2<-qplot(time,mz,data = p.,color=color,alpha=sz)
  p3<-qplot(mz,intensity,data = p.,color=color,alpha=sz)
  p4<-qplot(mz,ion,data=p.,color=color,alpha=sz)
  print(multiplot(p1, p2, p3,p4, cols=1))
  qplot(mz,intensity,data = p.,facets =factor(ct2$cluster)~.,color=color,alpha=sz)
  qualityTab$medOutlier[i]<-median(ct2$outlier_scores,na.rm = TRUE)
  qualityTab$numClass[i]<-max(ct2$cluster)
  qualityTab$numZero[i]<-length(which(ct2$cluster==0))
  qualityTab$N[i]<-dim(p.)[1]
  qualityTab$ppmSD[i]<-1e6*sd(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$ppmRange[i]<-1e6*diff(range(p.$mz))/maxMZ
  qualityTab$ppmIQR[i]<-1e6*IQR(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$logOutlier<-log10(qualityTab$medOutlier)
  qualityTab$Range2SD<-qualityTab$ppmRange/qualityTab$ppmSD
  qualityTab$SD2IQR<-qualityTab$ppmSD/qualityTab$ppmIQR
  predict(model2, newdata = (qualityTab[,-1]))->pred0
  summary(pred0)
  table(pred0)
```

## MZ=890.6294
```{r get.mz.890,echo=FALSE}
  idx<-which(abs(p$mz-890.6294)<1e-3)
  cP<-which.max(p$intensity[idx])
  ionI<-idx[cP]
  maxMZ<-p$mz[ionI]
```


```{r get.subset.890,echo=FALSE}
  idx <- which(abs(p$mz - maxMZ) < (ppm * 1e-6 * maxMZ))
  p. <- p[idx, ]
```

```{r cluster.mz.890,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  dissim1 = dist(p.$mz)
  dendro1 <- hclust(d = dissim1, method = 'ward.D2')
  ct1 <- cutreeDynamic(
    dendro1,
    cutHeight = NULL,
    minClusterSize = 30,
    method = "hybrid",
    deepSplit = 0,
    pamStage = TRUE,
    distM = as.matrix(dissim1),
    maxPamDist = 0,
    verbose = 0
  )
  cct1 <- ct1[which.min(abs(p.$mz - maxMZ))]
  id.1 <- idx[ct1 == cct1]
  mz1 <- median(p$mz[id.1])
  p.$color<-.pal[ct1+1]
  rl <- get_runLen(p$scan[id.1], scanLen)
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),log='y',color=color)
  p2<-qplot(time,mz,data = p.,color=color)
  p3<-qplot(mz,intensity,data = p.,color=color)
  print(multiplot(p1, p2, p3, cols=1))
  
```


```{r fit.lin.890,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  l<-lm(mz ~ time, data = p[id.1, ])
if (FALSE){#all(!is.na(l$coefficients))) {
  op <- par(mfrow = c(2, 2), oma = c(0, 0, 2, 0))
  plot(l)
  par(op)
}
```

```{r filter.lm.890,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
if (all(!is.na(l$coefficients))) {
  range(l$residuals[abs(rstandard(l)) <= 2]) -> bnd
  pl <- predict(l, p.)
  rl <- p.$mz - pl
  idL <- which(rl >= bnd[1] & rl <= bnd[2])
  p5 = qplot(
          time,
          mz,
          data = p.[idL],
          color = color,
          xlim = range(p.$time),
          ylim = range(p.$mz)
  )
  p6 = qplot(time,
          mz,
          data = p.,
          color = color) +
      geom_abline(intercept = l$coefficients[1],
          slope = l$coefficients[2])
  
  p.$resPPM<-rl*1e6/l$coefficients[1]
  p7<-qplot(resPPM,data=p.[idL],
            binwidth=binwidth,color=color)+
    stat_function(fun = function(x, mean, sd, n, bw){
      dnorm(x = x, mean = mean, sd = sd) * n * bw
      },
      args = c(mean = mean(p.$resPPM[idL]), 
               sd = sd(p.$resPPM[idL]), 
               n = length(idL), 
               bw = binwidth))
  print(multiplot(p5, p6, p7, cols = 1))
}
```

```{r get.quality.890,echo=FALSE,eval=FALSE}
  qualityTab<-data.frame(ion=maxMZ,
                         medOutlier=NA,
                         numClass=NA,
                         numZero=NA,
                         N=NA,
                         ppmSD=NA,
                         ppmRange=NA,
                         ppmIQR=NA)
  i<-1
  ct2 <- hdbscan(scale(p.[,c('mz')]), minPts = min(.minPts,dim(p.)[1]/4))
  p.$color<-.pal[ct2$cluster+1]
  p.$sz<-ct2$membership_prob
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),color=color,alpha=sz,log='y')
  p2<-qplot(time,mz,data = p.,color=color,alpha=sz)
  p3<-qplot(mz,intensity,data = p.,color=color,alpha=sz)
  p4<-qplot(mz,ion,data=p.,color=color,alpha=sz)
  print(multiplot(p1, p2, p3,p4, cols=1))
  qplot(mz,intensity,data = p.,facets =factor(ct2$cluster)~.,color=color,alpha=sz)
  qualityTab$medOutlier[i]<-median(ct2$outlier_scores,na.rm = TRUE)
  qualityTab$numClass[i]<-max(ct2$cluster)
  qualityTab$numZero[i]<-length(which(ct2$cluster==0))
  qualityTab$N[i]<-dim(p.)[1]
  qualityTab$ppmSD[i]<-1e6*sd(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$ppmRange[i]<-1e6*diff(range(p.$mz))/maxMZ
  qualityTab$ppmIQR[i]<-1e6*IQR(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$logOutlier<-log10(qualityTab$medOutlier)
  qualityTab$Range2SD<-qualityTab$ppmRange/qualityTab$ppmSD
  qualityTab$SD2IQR<-qualityTab$ppmSD/qualityTab$ppmIQR
  predict(model2, newdata = (qualityTab[,-1]))->pred0
  summary(pred0)
  table(pred0)
```



# Rejected continious features
## MZ=646.276916
```{r get.mz.646,echo=FALSE}
  idx<-which(abs(p$mz-646.276916)<1e-3)
  cP<-which.max(p$intensity[idx])
  ionI<-idx[cP]
  maxMZ<-p$mz[ionI]
```

```{r get.subset.646,echo=FALSE}
  idx <- which(abs(p$mz - maxMZ) < (ppm * 1e-6 * maxMZ))
  p. <- p[idx, ]
```

```{r cluster.mz.646,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  dissim1 = dist(p.$mz)
  dendro1 <- hclust(d = dissim1, method = 'ward.D2')
  ct1 <- cutreeDynamic(
    dendro1,
    cutHeight = NULL,
    minClusterSize = 30,
    method = "hybrid",
    deepSplit = 0,
    pamStage = TRUE,
    distM = as.matrix(dissim1),
    maxPamDist = 0,
    verbose = 0
  )
  cct1 <- ct1[which.min(abs(p.$mz - maxMZ))]
  id.1 <- idx[ct1 == cct1]
  mz1 <- median(p$mz[id.1])
  p.$color<-.pal[ct1+1]
  rl <- get_runLen(p$scan[id.1], scanLen)
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),log='y',color=color)
  p2<-qplot(time,mz,data = p.,color=color)
  p3<-qplot(mz,intensity,data = p.,color=color)
  print(multiplot(p1, p2, p3, cols=1))
  
```


```{r fit.lin.646,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  l<-lm(mz ~ time, data = p[id.1, ])
if (FALSE){#all(!is.na(l$coefficients))) {
  op <- par(mfrow = c(2, 2), oma = c(0, 0, 2, 0))
  plot(l)
  par(op)
}
```

```{r filter.lm.646,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
if (all(!is.na(l$coefficients))) {
  range(l$residuals[abs(rstandard(l)) <= 2]) -> bnd
  pl <- predict(l, p.)
  rl <- p.$mz - pl
  idL <- which(rl >= bnd[1] & rl <= bnd[2])
  p5 = qplot(
          time,
          mz,
          data = p.[idL],
          color = color,
          xlim = range(p.$time),
          ylim = range(p.$mz)
  )
  p6 = qplot(time,
          mz,
          data = p.,
          color = color) +
      geom_abline(intercept = l$coefficients[1],
          slope = l$coefficients[2])
  
  p.$resPPM<-rl*1e6/l$coefficients[1]
  p7<-qplot(resPPM,data=p.[idL],
            binwidth=binwidth,color=color)+
    stat_function(fun = function(x, mean, sd, n, bw){
      dnorm(x = x, mean = mean, sd = sd) * n * bw
      },
      args = c(mean = mean(p.$resPPM[idL]), 
               sd = sd(p.$resPPM[idL]), 
               n = length(idL), 
               bw = binwidth))
  print(multiplot(p5, p6, p7, cols = 1))
}
```

```{r get.quality.646,echo=FALSE,eval=FALSE}
  qualityTab<-data.frame(ion=maxMZ,
                         medOutlier=NA,
                         numClass=NA,
                         numZero=NA,
                         N=NA,
                         ppmSD=NA,
                         ppmRange=NA,
                         ppmIQR=NA)
  i<-1
  ct2 <- hdbscan(scale(p.[,c('mz')]), minPts = min(.minPts,dim(p.)[1]/4))
  p.$color<-.pal[ct2$cluster+1]
  p.$sz<-ct2$membership_prob
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),color=color,alpha=sz,log='y')
  p2<-qplot(time,mz,data = p.,color=color,alpha=sz)
  p3<-qplot(mz,intensity,data = p.,color=color,alpha=sz)
  p4<-qplot(mz,ion,data=p.,color=color,alpha=sz)
  print(multiplot(p1, p2, p3,p4, cols=1))
  qplot(mz,intensity,data = p.,facets =factor(ct2$cluster)~.,color=color,alpha=sz)
  qualityTab$medOutlier[i]<-median(ct2$outlier_scores,na.rm = TRUE)
  qualityTab$numClass[i]<-max(ct2$cluster)
  qualityTab$numZero[i]<-length(which(ct2$cluster==0))
  qualityTab$N[i]<-dim(p.)[1]
  qualityTab$ppmSD[i]<-1e6*sd(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$ppmRange[i]<-1e6*diff(range(p.$mz))/maxMZ
  qualityTab$ppmIQR[i]<-1e6*IQR(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$logOutlier<-log10(qualityTab$medOutlier)
  qualityTab$Range2SD<-qualityTab$ppmRange/qualityTab$ppmSD
  qualityTab$SD2IQR<-qualityTab$ppmSD/qualityTab$ppmIQR
  predict(model2, newdata = (qualityTab[,-1]))->pred0
  summary(pred0)
  table(pred0)
```




## MZ=430.6063
```{r get.mz.430,echo=FALSE}
  idx<-which(abs(p$mz-430.6063)<1e-3)
  cP<-which.max(p$intensity[idx])
  ionI<-idx[cP]
  maxMZ<-p$mz[ionI]
```

```{r get.subset.430,echo=FALSE}
  idx <- which(abs(p$mz - maxMZ) < (ppm * 1e-6 * maxMZ))
  p. <- p[idx, ]
```

```{r cluster.mz.430,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  dissim1 = dist(p.$mz)
  dendro1 <- hclust(d = dissim1, method = 'ward.D2')
  ct1 <- cutreeDynamic(
    dendro1,
    cutHeight = NULL,
    minClusterSize = 30,
    method = "hybrid",
    deepSplit = 0,
    pamStage = TRUE,
    distM = as.matrix(dissim1),
    maxPamDist = 0,
    verbose = 0
  )
  cct1 <- ct1[which.min(abs(p.$mz - maxMZ))]
  id.1 <- idx[ct1 == cct1]
  mz1 <- median(p$mz[id.1])
  p.$color<-.pal[ct1+1]
  rl <- get_runLen(p$scan[id.1], scanLen)
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),log='y',color=color)
  p2<-qplot(time,mz,data = p.,color=color)
  p3<-qplot(mz,intensity,data = p.,color=color)
  print(multiplot(p1, p2, p3, cols=1))
  
```


```{r fit.lin.430,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  l<-lm(mz ~ time, data = p[id.1, ])
if (FALSE){#all(!is.na(l$coefficients))) {
  op <- par(mfrow = c(2, 2), oma = c(0, 0, 2, 0))
  plot(l)
  par(op)
}
```

```{r filter.lm.430,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
if (all(!is.na(l$coefficients))) {
  range(l$residuals[abs(rstandard(l)) <= 2]) -> bnd
  pl <- predict(l, p.)
  rl <- p.$mz - pl
  idL <- which(rl >= bnd[1] & rl <= bnd[2])
  p5 = qplot(
          time,
          mz,
          data = p.[idL],
          color = color,
          xlim = range(p.$time),
          ylim = range(p.$mz)
  )
  p6 = qplot(time,
          mz,
          data = p.,
          color = color) +
      geom_abline(intercept = l$coefficients[1],
          slope = l$coefficients[2])
  
  p.$resPPM<-rl*1e6/l$coefficients[1]
  p7<-qplot(resPPM,data=p.[idL],
            binwidth=binwidth,color=color)+
    stat_function(fun = function(x, mean, sd, n, bw){
      dnorm(x = x, mean = mean, sd = sd) * n * bw
      },
      args = c(mean = mean(p.$resPPM[idL]), 
               sd = sd(p.$resPPM[idL]), 
               n = length(idL), 
               bw = binwidth))
  print(multiplot(p5, p6, p7, cols = 1))
}
```

```{r get.quality.430,echo=FALSE,eval=FALSE}
  qualityTab<-data.frame(ion=maxMZ,
                         medOutlier=NA,
                         numClass=NA,
                         numZero=NA,
                         N=NA,
                         ppmSD=NA,
                         ppmRange=NA,
                         ppmIQR=NA)
  i<-1
  ct2 <- hdbscan(scale(p.[,c('mz')]), minPts = min(.minPts,dim(p.)[1]/4))
  p.$color<-.pal[ct2$cluster+1]
  p.$sz<-ct2$membership_prob
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),color=color,alpha=sz,log='y')
  p2<-qplot(time,mz,data = p.,color=color,alpha=sz)
  p3<-qplot(mz,intensity,data = p.,color=color,alpha=sz)
  p4<-qplot(mz,ion,data=p.,color=color,alpha=sz)
  print(multiplot(p1, p2, p3,p4, cols=1))
  qplot(mz,intensity,data = p.,facets =factor(ct2$cluster)~.,color=color,alpha=sz)
  qualityTab$medOutlier[i]<-median(ct2$outlier_scores,na.rm = TRUE)
  qualityTab$numClass[i]<-max(ct2$cluster)
  qualityTab$numZero[i]<-length(which(ct2$cluster==0))
  qualityTab$N[i]<-dim(p.)[1]
  qualityTab$ppmSD[i]<-1e6*sd(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$ppmRange[i]<-1e6*diff(range(p.$mz))/maxMZ
  qualityTab$ppmIQR[i]<-1e6*IQR(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$logOutlier<-log10(qualityTab$medOutlier)
  qualityTab$Range2SD<-qualityTab$ppmRange/qualityTab$ppmSD
  qualityTab$SD2IQR<-qualityTab$ppmSD/qualityTab$ppmIQR
  predict(model2, newdata = (qualityTab[,-1]))->pred0
  summary(pred0)
  table(pred0)
```


## MZ=1261.3767
```{r get.mz.1261,echo=FALSE}
  idx<-which(abs(p$mz-1261.3767)<1e-3)
  cP<-which.max(p$intensity[idx])
  ionI<-idx[cP]
  maxMZ<-p$mz[ionI]
```


```{r get.subset.1261,echo=FALSE}
  idx <- which(abs(p$mz - maxMZ) < (ppm * 1e-6 * maxMZ))
  p. <- p[idx, ]
```

```{r cluster.mz.1261,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  dissim1 = dist(p.$mz)
  dendro1 <- hclust(d = dissim1, method = 'ward.D2')
  ct1 <- cutreeDynamic(
    dendro1,
    cutHeight = NULL,
    minClusterSize = 30,
    method = "hybrid",
    deepSplit = 0,
    pamStage = TRUE,
    distM = as.matrix(dissim1),
    maxPamDist = 0,
    verbose = 0
  )
  cct1 <- ct1[which.min(abs(p.$mz - maxMZ))]
  id.1 <- idx[ct1 == cct1]
  mz1 <- median(p$mz[id.1])
  p.$color<-.pal[ct1+1]
  rl <- get_runLen(p$scan[id.1], scanLen)
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),log='y',color=color)
  p2<-qplot(time,mz,data = p.,color=color)
  p3<-qplot(mz,intensity,data = p.,color=color)
  print(multiplot(p1, p2, p3, cols=1))
  
```


```{r fit.lin.1261,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  l<-lm(mz ~ time, data = p[id.1, ])
if (FALSE){#all(!is.na(l$coefficients))) {
  op <- par(mfrow = c(2, 2), oma = c(0, 0, 2, 0))
  plot(l)
  par(op)
}
```

```{r filter.lm.1261,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
if (all(!is.na(l$coefficients))) {
  range(l$residuals[abs(rstandard(l)) <= 2]) -> bnd
  pl <- predict(l, p.)
  rl <- p.$mz - pl
  idL <- which(rl >= bnd[1] & rl <= bnd[2])
  p5 = qplot(
          time,
          mz,
          data = p.[idL],
          color = color,
          xlim = range(p.$time),
          ylim = range(p.$mz)
  )
  p6 = qplot(time,
          mz,
          data = p.,
          color = color) +
      geom_abline(intercept = l$coefficients[1],
          slope = l$coefficients[2])
  
  p.$resPPM<-rl*1e6/l$coefficients[1]
  p7<-qplot(resPPM,data=p.[idL],
            binwidth=binwidth,color=color)+
    stat_function(fun = function(x, mean, sd, n, bw){
      dnorm(x = x, mean = mean, sd = sd) * n * bw
      },
      args = c(mean = mean(p.$resPPM[idL]), 
               sd = sd(p.$resPPM[idL]), 
               n = length(idL), 
               bw = binwidth))
  print(multiplot(p5, p6, p7, cols = 1))
}
```

```{r get.quality.1261,echo=FALSE,eval=FALSE}
  qualityTab<-data.frame(ion=maxMZ,
                         medOutlier=NA,
                         numClass=NA,
                         numZero=NA,
                         N=NA,
                         ppmSD=NA,
                         ppmRange=NA,
                         ppmIQR=NA)
  i<-1
  ct2 <- hdbscan(scale(p.[,c('mz')]), minPts = min(.minPts,dim(p.)[1]/4))
  p.$color<-.pal[ct2$cluster+1]
  p.$sz<-ct2$membership_prob
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),color=color,alpha=sz,log='y')
  p2<-qplot(time,mz,data = p.,color=color,alpha=sz)
  p3<-qplot(mz,intensity,data = p.,color=color,alpha=sz)
  p4<-qplot(mz,ion,data=p.,color=color,alpha=sz)
  print(multiplot(p1, p2, p3,p4, cols=1))
  qplot(mz,intensity,data = p.,facets =factor(ct2$cluster)~.,color=color,alpha=sz)
  qualityTab$medOutlier[i]<-median(ct2$outlier_scores,na.rm = TRUE)
  qualityTab$numClass[i]<-max(ct2$cluster)
  qualityTab$numZero[i]<-length(which(ct2$cluster==0))
  qualityTab$N[i]<-dim(p.)[1]
  qualityTab$ppmSD[i]<-1e6*sd(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$ppmRange[i]<-1e6*diff(range(p.$mz))/maxMZ
  qualityTab$ppmIQR[i]<-1e6*IQR(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$logOutlier<-log10(qualityTab$medOutlier)
  qualityTab$Range2SD<-qualityTab$ppmRange/qualityTab$ppmSD
  qualityTab$SD2IQR<-qualityTab$ppmSD/qualityTab$ppmIQR
  predict(model2, newdata = (qualityTab[,-1]))->pred0
  summary(pred0)
  table(pred0)
```

## MZ=1110.5463
```{r get.mz.1110,echo=FALSE}
  idx<-which(abs(p$mz-1110.5463)<1e-3)
  cP<-which.max(p$intensity[idx])
  ionI<-idx[cP]
  maxMZ<-p$mz[ionI]
```

```{r get.subset.1110,echo=FALSE}
  idx <- which(abs(p$mz - maxMZ) < (ppm * 1e-6 * maxMZ))
  p. <- p[idx, ]
```

```{r cluster.mz.1110,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  dissim1 = dist(p.$mz)
  dendro1 <- hclust(d = dissim1, method = 'ward.D2')
  ct1 <- cutreeDynamic(
    dendro1,
    cutHeight = NULL,
    minClusterSize = 30,
    method = "hybrid",
    deepSplit = 0,
    pamStage = TRUE,
    distM = as.matrix(dissim1),
    maxPamDist = 0,
    verbose = 0
  )
  cct1 <- ct1[which.min(abs(p.$mz - maxMZ))]
  id.1 <- idx[ct1 == cct1]
  mz1 <- median(p$mz[id.1])
  p.$color<-.pal[ct1+1]
  rl <- get_runLen(p$scan[id.1], scanLen)
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),log='y',color=color)
  p2<-qplot(time,mz,data = p.,color=color)
  p3<-qplot(mz,intensity,data = p.,color=color)
  print(multiplot(p1, p2, p3, cols=1))
  
```


```{r fit.lin.1110,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  l<-lm(mz ~ time, data = p[id.1, ])
if (FALSE){#all(!is.na(l$coefficients))) {
  op <- par(mfrow = c(2, 2), oma = c(0, 0, 2, 0))
  plot(l)
  par(op)
}
```

```{r filter.lm.1110,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
if (all(!is.na(l$coefficients))) {
  range(l$residuals[abs(rstandard(l)) <= 2]) -> bnd
  pl <- predict(l, p.)
  rl <- p.$mz - pl
  idL <- which(rl >= bnd[1] & rl <= bnd[2])
  p5 = qplot(
          time,
          mz,
          data = p.[idL],
          color = color,
          xlim = range(p.$time),
          ylim = range(p.$mz)
  )
  p6 = qplot(time,
          mz,
          data = p.,
          color = color) +
      geom_abline(intercept = l$coefficients[1],
          slope = l$coefficients[2])
  
  p.$resPPM<-rl*1e6/l$coefficients[1]
  p7<-qplot(resPPM,data=p.[idL],
            binwidth=binwidth,color=color)+
    stat_function(fun = function(x, mean, sd, n, bw){
      dnorm(x = x, mean = mean, sd = sd) * n * bw
      },
      args = c(mean = mean(p.$resPPM[idL]), 
               sd = sd(p.$resPPM[idL]), 
               n = length(idL), 
               bw = binwidth))
  print(multiplot(p5, p6, p7, cols = 1))
}
```

```{r get.quality.1110,echo=FALSE,eval=FALSE}
  qualityTab<-data.frame(ion=maxMZ,
                         medOutlier=NA,
                         numClass=NA,
                         numZero=NA,
                         N=NA,
                         ppmSD=NA,
                         ppmRange=NA,
                         ppmIQR=NA)
  i<-1
  ct2 <- hdbscan(scale(p.[,c('mz')]), minPts = min(.minPts,dim(p.)[1]/4))
  p.$color<-.pal[ct2$cluster+1]
  p.$sz<-ct2$membership_prob
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),color=color,alpha=sz,log='y')
  p2<-qplot(time,mz,data = p.,color=color,alpha=sz)
  p3<-qplot(mz,intensity,data = p.,color=color,alpha=sz)
  p4<-qplot(mz,ion,data=p.,color=color,alpha=sz)
  print(multiplot(p1, p2, p3,p4, cols=1))
  qplot(mz,intensity,data = p.,facets =factor(ct2$cluster)~.,color=color,alpha=sz)
  qualityTab$medOutlier[i]<-median(ct2$outlier_scores,na.rm = TRUE)
  qualityTab$numClass[i]<-max(ct2$cluster)
  qualityTab$numZero[i]<-length(which(ct2$cluster==0))
  qualityTab$N[i]<-dim(p.)[1]
  qualityTab$ppmSD[i]<-1e6*sd(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$ppmRange[i]<-1e6*diff(range(p.$mz))/maxMZ
  qualityTab$ppmIQR[i]<-1e6*IQR(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$logOutlier<-log10(qualityTab$medOutlier)
  qualityTab$Range2SD<-qualityTab$ppmRange/qualityTab$ppmSD
  qualityTab$SD2IQR<-qualityTab$ppmSD/qualityTab$ppmIQR
  predict(model2, newdata = (qualityTab[,-1]))->pred0
  summary(pred0)
  table(pred0)
```


## MZ=222.631927
```{r get.mz.222,echo=FALSE}
  idx<-which(abs(p$mz-222.631927)<1e-3)
  cP<-which.max(p$intensity[idx])
  ionI<-idx[cP]
  maxMZ<-p$mz[ionI]
```

```{r get.subset.222,echo=FALSE}
  idx <- which(abs(p$mz - maxMZ) < (ppm * 1e-6 * maxMZ))
  p. <- p[idx, ]
```

```{r cluster.mz.222,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  dissim1 = dist(p.$mz)
  dendro1 <- hclust(d = dissim1, method = 'ward.D2')
  ct1 <- cutreeDynamic(
    dendro1,
    cutHeight = NULL,
    minClusterSize = 30,
    method = "hybrid",
    deepSplit = 0,
    pamStage = TRUE,
    distM = as.matrix(dissim1),
    maxPamDist = 0,
    verbose = 0
  )
  cct1 <- ct1[which.min(abs(p.$mz - maxMZ))]
  id.1 <- idx[ct1 == cct1]
  mz1 <- median(p$mz[id.1])
  p.$color<-.pal[ct1+1]
  rl <- get_runLen(p$scan[id.1], scanLen)
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),log='y',color=color)
  p2<-qplot(time,mz,data = p.,color=color)
  p3<-qplot(mz,intensity,data = p.,color=color)
  print(multiplot(p1, p2, p3, cols=1))
  
```


```{r fit.lin.222,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
  l<-lm(mz ~ time, data = p[id.1, ])
if (FALSE){#all(!is.na(l$coefficients))) {
  op <- par(mfrow = c(2, 2), oma = c(0, 0, 2, 0))
  plot(l)
  par(op)
}
```

```{r filter.lm.222,fig.width=10.5,fig.height=10.5,results='asis',cache=FALSE,echo=FALSE}
if (all(!is.na(l$coefficients))) {
  range(l$residuals[abs(rstandard(l)) <= 2]) -> bnd
  pl <- predict(l, p.)
  rl <- p.$mz - pl
  idL <- which(rl >= bnd[1] & rl <= bnd[2])
  p5 = qplot(
          time,
          mz,
          data = p.[idL],
          color = color,
          xlim = range(p.$time),
          ylim = range(p.$mz)
  )
  p6 = qplot(time,
          mz,
          data = p.,
          color = color) +
      geom_abline(intercept = l$coefficients[1],
          slope = l$coefficients[2])
  
  p.$resPPM<-rl*1e6/l$coefficients[1]
  p7<-qplot(resPPM,data=p.[idL],
            binwidth=binwidth,color=color)+
    stat_function(fun = function(x, mean, sd, n, bw){
      dnorm(x = x, mean = mean, sd = sd) * n * bw
      },
      args = c(mean = mean(p.$resPPM[idL]), 
               sd = sd(p.$resPPM[idL]), 
               n = length(idL), 
               bw = binwidth))
  print(multiplot(p5, p6, p7, cols = 1))
}
```

```{r get.quality.222,echo=FALSE,eval=FALSE}
  qualityTab<-data.frame(ion=maxMZ,
                         medOutlier=NA,
                         numClass=NA,
                         numZero=NA,
                         N=NA,
                         ppmSD=NA,
                         ppmRange=NA,
                         ppmIQR=NA)
  i<-1
  ct2 <- hdbscan(scale(p.[,c('mz')]), minPts = min(.minPts,dim(p.)[1]/4))
  p.$color<-.pal[ct2$cluster+1]
  p.$sz<-ct2$membership_prob
  p1<-qplot(time,intensity,data = p.,main=paste0('mz',maxMZ),color=color,alpha=sz,log='y')
  p2<-qplot(time,mz,data = p.,color=color,alpha=sz)
  p3<-qplot(mz,intensity,data = p.,color=color,alpha=sz)
  p4<-qplot(mz,ion,data=p.,color=color,alpha=sz)
  print(multiplot(p1, p2, p3,p4, cols=1))
  qplot(mz,intensity,data = p.,facets =factor(ct2$cluster)~.,color=color,alpha=sz)
  qualityTab$medOutlier[i]<-median(ct2$outlier_scores,na.rm = TRUE)
  qualityTab$numClass[i]<-max(ct2$cluster)
  qualityTab$numZero[i]<-length(which(ct2$cluster==0))
  qualityTab$N[i]<-dim(p.)[1]
  qualityTab$ppmSD[i]<-1e6*sd(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$ppmRange[i]<-1e6*diff(range(p.$mz))/maxMZ
  qualityTab$ppmIQR[i]<-1e6*IQR(p.$mz,na.rm = TRUE)/maxMZ
  qualityTab$logOutlier<-log10(qualityTab$medOutlier)
  qualityTab$Range2SD<-qualityTab$ppmRange/qualityTab$ppmSD
  qualityTab$SD2IQR<-qualityTab$ppmSD/qualityTab$ppmIQR
  predict(model2, newdata = (qualityTab[,-1]))->pred0
  summary(pred0)
  table(pred0)
```




# Appendix {.tabset}
## Custom Functions
```{r functions, eval=FALSE, include=TRUE}
```

## Version
### Document version
```{r docVersion, echo=FALSE, results='asis', cache=FALSE}
cat(params$version,'\n',format(Sys.time(), "%d.%m.%Y"))

```

### Session Info
```{r sessionInfo, echo=FALSE, results='asis', class='text', warning=FALSE}
pander(devtools::session_info())
```





